{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/CarlLee1983/Tagsmith/schema.json",
  "title": "Tagsmith config",
  "description": "Tag specification for the tagsmith CLI (.tagsmith.json).",
  "oneOf": [
    {
      "title": "Multi-line configuration",
      "type": "object",
      "required": ["tags"],
      "additionalProperties": false,
      "properties": {
        "$schema": { "type": "string" },
        "tags": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/definitions/tagLine" }
        },
        "default": {
          "type": "string",
          "description": "Default tag-line name; defaults to the first line."
        },
        "mergePolicy": { "$ref": "#/definitions/mergePolicy" },
        "releasePolicy": { "$ref": "#/definitions/releasePolicy" },
        "commitPolicy": { "$ref": "#/definitions/commitPolicy" }
      }
    },
    {
      "title": "Legacy single-line configuration",
      "type": "object",
      "required": ["pattern", "model", "initialVersion"],
      "additionalProperties": false,
      "properties": {
        "$schema": { "type": "string" },
        "pattern": { "$ref": "#/definitions/pattern" },
        "model": { "$ref": "#/definitions/model" },
        "initialVersion": {
          "type": "string",
          "minLength": 1,
          "description": "Version used when no conforming tag exists yet."
        },
        "push": {
          "type": "boolean",
          "description": "Default push behaviour for `tagsmith create`."
        },
        "workspace": {
          "type": "string",
          "minLength": 1,
          "allOf": [
            { "pattern": "^(?![\\\\/])(?![A-Za-z]:[\\\\/]).+" },
            { "not": { "pattern": "(^|[\\\\/])\\.\\.([\\\\/]|$)" } }
          ],
          "description": "Repository-relative workspace path for --require-changes."
        },
        "artifact": { "$ref": "#/definitions/artifact" },
        "mergePolicy": { "$ref": "#/definitions/mergePolicy" },
        "releasePolicy": { "$ref": "#/definitions/releasePolicy" },
        "commitPolicy": { "$ref": "#/definitions/commitPolicy" }
      }
    }
  ],
  "definitions": {
    "pattern": {
      "type": "string",
      "description": "Git-valid tag template containing exactly one {version} placeholder.",
      "allOf": [
        { "pattern": "^(?!.*\\{version\\}.*\\{version\\}).*\\{version\\}.*$" },
        { "not": { "pattern": "[\\u0000-\\u0020\\u007f~^:?*]" } },
        { "not": { "pattern": "\\[" } },
        { "not": { "pattern": "\\\\" } },
        { "not": { "pattern": "\\.\\.|@\\{|//|^[-/]|/$|\\.$|(^|/)\\.|\\.lock($|/)" } }
      ]
    },
    "model": {
      "oneOf": [
        {
          "type": "object",
          "required": ["type"],
          "additionalProperties": false,
          "properties": {
            "type": { "const": "semver" },
            "allowPrerelease": { "type": "boolean" }
          }
        },
        {
          "type": "object",
          "required": ["type", "format"],
          "additionalProperties": false,
          "properties": {
            "type": { "const": "calver" },
            "format": {
              "type": "string",
              "minLength": 1,
              "description": "Token sequence using YYYY YY MM DD MICRO, e.g. YYYY.MM.MICRO."
            }
          }
        },
        {
          "type": "object",
          "required": ["type"],
          "additionalProperties": false,
          "properties": {
            "type": { "const": "build" },
            "padding": { "type": "integer", "minimum": 0 }
          }
        }
      ]
    },
    "tagLine": {
      "type": "object",
      "required": ["name", "pattern", "model", "initialVersion"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Unique line name, used by --tag."
        },
        "workspace": {
          "type": "string",
          "minLength": 1,
          "allOf": [
            { "pattern": "^(?![\\\\/])(?![A-Za-z]:[\\\\/]).+" },
            { "not": { "pattern": "(^|[\\\\/])\\.\\.([\\\\/]|$)" } }
          ],
          "description": "Repository-relative workspace path for --require-changes."
        },
        "pattern": { "$ref": "#/definitions/pattern" },
        "model": { "$ref": "#/definitions/model" },
        "initialVersion": {
          "type": "string",
          "minLength": 1,
          "description": "Version used when no conforming tag exists yet."
        },
        "push": {
          "type": "boolean",
          "description": "Default push behaviour for `tagsmith create`."
        },
        "artifact": { "$ref": "#/definitions/artifact" }
      }
    },
    "mergePolicy": {
      "type": "object",
      "description": "Optional branch merge guardrails enforced by `tagsmith merge-check` via git hooks.",
      "additionalProperties": false,
      "required": ["protectedBranches"],
      "properties": {
        "protectedBranches": {
          "type": "object",
          "description": "Map of protected branch name -> merge rule. Only the current branch is checked; others pass.",
          "additionalProperties": {
            "type": "object",
            "description": "Set exactly one of allow (whitelist) or deny (blacklist). Sources support glob (* and ?).",
            "additionalProperties": false,
            "oneOf": [{ "required": ["allow"] }, { "required": ["deny"] }],
            "properties": {
              "allow": {
                "type": "array",
                "description": "Only these sources may merge in; all others are blocked.",
                "items": { "type": "string", "minLength": 1 }
              },
              "deny": {
                "type": "array",
                "description": "These sources are blocked; all others pass.",
                "items": { "type": "string", "minLength": 1 }
              }
            }
          }
        },
        "onUnknownSource": {
          "enum": ["block", "allow"],
          "default": "block",
          "description": "Behaviour when the merge source branch cannot be resolved."
        }
      }
    },
    "releasePolicy": {
      "type": "object",
      "description": "Optional local release readiness guardrails. They are enforced only by `tagsmith create --enforce-policy`.",
      "additionalProperties": false,
      "properties": {
        "allowedBranches": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 },
          "description": "Branch patterns allowed to create a release. Supports * and ? globs."
        },
        "requireCleanWorktree": {
          "type": "boolean",
          "default": false,
          "description": "Require no staged, unstaged, or untracked files."
        },
        "requireAnnotatedTag": {
          "type": "boolean",
          "default": false,
          "description": "Require the candidate to be an annotated tag."
        },
        "requireHeadTag": {
          "type": "boolean",
          "default": false,
          "description": "Require the candidate tag target to equal HEAD."
        },
        "signature": {
          "enum": ["optional", "required"],
          "default": "optional",
          "description": "Whether create must request a signed tag from Git."
        },
        "requireArtifactVersion": {
          "type": "boolean",
          "default": false,
          "description": "Require create --enforce-policy to match the candidate tag version with its configured artifact."
        }
      }
    },
    "artifact": {
      "type": "object",
      "description": "A built-in release artifact checked against tag versions.",
      "additionalProperties": false,
      "required": ["type"],
      "properties": {
        "type": {
          "const": "package-json",
          "description": "Read version from workspace/package.json, or package.json at repository root when workspace is omitted."
        }
      }
    },
    "commitPolicy": {
      "type": "object",
      "description": "Optional ordered Conventional Commit release rules. When present, unmatched commits are ignored.",
      "additionalProperties": false,
      "required": ["rules"],
      "properties": {
        "rules": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/definitions/commitRule" }
        }
      }
    },
    "commitRule": {
      "type": "object",
      "description": "First matching rule wins. Set exactly one of release or ignore: true.",
      "additionalProperties": false,
      "oneOf": [
        { "required": ["release"], "not": { "required": ["ignore"] } },
        { "required": ["ignore"], "not": { "required": ["release"] } }
      ],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "type": { "type": "string", "minLength": 1 },
        "scope": { "type": "string", "minLength": 1 },
        "breaking": { "type": "boolean" },
        "release": { "enum": ["major", "minor", "patch"] },
        "ignore": { "const": true }
      }
    }
  }
}
